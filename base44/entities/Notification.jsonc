{
  "name": "Notification",
  "type": "object",
  "properties": {
    "user_email": {
      "type": "string",
      "description": "Recipient user email"
    },
    "school_id": {
      "type": "string"
    },
    "title": {
      "type": "string"
    },
    "body": {
      "type": "string"
    },
    "type": {
      "type": "string",
      "enum": [
        "announcement_urgent",
        "announcement_important",
        "announcement_scheduled_reminder",
        "message",
        "endorsement",
        "follow",
        "request_signed_off",
        "request_changes",
        "team_accepted",
        "opportunity_match",
        "view_milestone",
        "org_approved"
      ],
      "description": "Notification type"
    },
    "priority": {
      "type": "string",
      "enum": [
        "normal",
        "important",
        "urgent"
      ],
      "default": "normal"
    },
    "related_id": {
      "type": "string",
      "description": "ID of the related entity (e.g. announcement ID, message ID, registry ID, membership ID)"
    },
    "read": {
      "type": "boolean",
      "default": false
    },
    "read_at": {
      "type": "string",
      "format": "date-time"
    },
    "digested_at": {
      "type": "string",
      "format": "date-time",
      "description": "Set by the daily digest job once this notification has been included in (or deliberately skipped from) a digest email \u2014 prevents re-sending. Unread social/credential notifications without this are pending their daily digest."
    }
  },
  "required": [
    "user_email",
    "title",
    "body",
    "type"
  ],
  "rls": {
    "create": {
      "data.school_id": "__service_role_only__"
    },
    "read": {
      "data.user_email": "{{user.email}}"
    },
    "update": {
      "data.user_email": "{{user.email}}"
    },
    "delete": {
      "user_condition": {
        "role": "admin"
      },
      "data.school_id": "{{user.profile.school_id}}"
    }
  }
}